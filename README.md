# cambiumMarsRoverBackend. This is a dotnet 6 api application. The frontend built with angular 14 would connect when both applications are running via the exposed port, then the test could be tested.
